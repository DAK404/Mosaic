/**
* Contains the dependencies which are used to download, encrypt, decrypt the files when requested by the user.
*
* Encrypt requires the user to enter a password to encrypt a file to a LOCK file.
* Decrypt requires the user to enter the password which was used to encrypt the file to decrypt it.
*/

package API.Download;
